function display(weather, top, bottom, accessory)
{
  document.body.innerHTML = "It is " + weather + ". You should take "+ scarf +". You should wear a "+ top + " and a " + bottom;

}
function test (){

  alert("hi");
  var t= document.getElementById(temp).text;
  var l= document.getElementById(loc).value;
  alert(l);
  alert(t);
}
